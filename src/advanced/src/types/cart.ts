export type ProductModel = {
  id: string;
  name: string;
  price: number;
  quantity: number;
};

export type CartItemModel = {
  id: string;
  name: string;
  price: number;
  cartQuantity: number;
};
