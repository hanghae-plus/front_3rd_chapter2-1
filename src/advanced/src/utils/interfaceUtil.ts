export interface Product {
  id: string;
  name: string;
  price: number;
  quantity: number;
  discountRate: number;
}

export interface Item {
  id: string;
  quantity: number;
  name: string;
  price: number;
}
